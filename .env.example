# Optional override for persisted data (Railway volume default: /data)
# DATA_DIR=./data

# Shared library ID — all devices sync to the same backlog file
# LIBRARY_SYNC_ID=mysteam-library

# IGDB (Twitch) — used for covers, screenshots, and artwork
# https://api-docs.igdb.com/
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=
